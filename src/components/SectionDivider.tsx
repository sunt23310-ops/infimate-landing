export default function SectionDivider() {
  return (
    <div className="relative w-full h-px">
      <div className="absolute inset-0 bg-gradient-to-r from-transparent via-[var(--purple-primary)]/20 to-transparent" />
    </div>
  );
}
